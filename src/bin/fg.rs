/// fg stands for flaggrep. it's a tool that searches a directory structure for ctf flags of a given format.
/// it searches binary files for the flag format or the flag format in a variety of encodings.
/// it returns any matches to stdout.
#[tokio::main]
pub async fn main() -> anyhow::Result<()> {
    todo!()
}
